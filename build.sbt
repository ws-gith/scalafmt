ThisBuild / scalaVersion := "3.5.2"
ThisBuild / version := "0.1.0"
ThisBuild / organization := "org.nextcamp"

lazy val root = (project in file("."))
  .settings(
    libraryDependencies ++= Seq(
      // Your dependencies will go here
    )
  )
